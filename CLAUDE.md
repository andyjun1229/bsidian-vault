# 个人知识库运行手册

raw → you → outputs 三级知识加工系统。不是文件夹，是流水线。

```
    输入端                      加工层                         输出端
  ┌──────────┐           ┌──────────────────┐          ┌──────────────┐
  │  飞书转发  │           │  raw/  外部素材库   │          │  outputs/    │
  │  手动输入  │───→ Hermes───→  you/  知识图谱+灵感  │───→ 导航索引     │
  │  对话记录  │  调度中枢   │                  │          │  公众号文章   │
  └──────────┘           └──────────────────┘          └──────────────┘
                                │         ↑
                                ▼         │
                           ┌──────────────────┐
                           │  校验 + 遗忘      │
                           │  validate_entry  │
                           │  30天不碰→stale  │
                           └──────────────────┘
```

---

## 架构

### raw/ — 外部素材库（只进不改）

装别人写的东西。**你自己的文章不进这里。**

```yaml
---
来源：公众号/网页/视频/X/书籍/PRD
链接：URL
时间：YYYY-MM-DD
平台：公众号/网页/视频/X/书籍/PRD/其他
---
```

- 一素材一文件，原文不做语义改写
- 命名：`YYYY-MM-DD--来源分类--主题.md`
  - `来源分类` 取自以下标准值：公众号、自己创作、AI实操、AI思考、AI技术、投资、写书、自我访谈、项目、养生、行动哲学、思考、方法论、操作经验、操作日志、读书笔记、B站、网页、GitHub、微信视频、小宇宙、OpenAI
  - raw/ 来源分类用外部平台名（公众号、B站、GitHub、网页、小宇宙、OpenAI等）
  - you/ 来源分类用内容性质（公众号、自己创作、AI实操、AI思考、AI技术、投资、写书、自我访谈、灵感等）
- 看到 `平台：自己创作` → 这是旧架构遗留，搬去 you/

### you/ — 个人知识图谱（核心资产）

你的思想产物。这是 vault 的重心。

**装什么：**
- 你的公众号文章（保留原始表达）
- 你的方法论/框架/原则
- 你的操作经验（"听了→用了→产出"）
- 你的融合观点（读了A+学了B+自己实践→独特认知）
- 你直接用过的东西（看了手册配好了系统，没写文章但值得记）

**每个文件必须有元数据：**

```yaml
---
type: [article|method|note|experience|prd|story|archive|draft]
status: [evergreen|growing|sprouting|stale]
tags: [分类1, 分类2]  # 至少一个，从来源分类标准值中选，可多标签
---
```

| type | 含义 | 典型内容 |
|------|------|---------|
| article | 公众号文章 / 完整文章 | 正式输出 |
| method | 方法论 / 框架 / 原则 | 可复用做事方法 |
| note | 笔记 / 读后感 / 想法 | 相对随意记录 |
| experience | 操作经验 / 实战记录 | 做了什么、效果如何 |
| prd | 产品需求文档 / 方案 | PRD、方案设计 |
| story | 回忆录 / 自传 | 非技术内容 |
| archive | 一次性归档 / 报告 | 分类报告 |
| draft | 草稿 / 待补素材 | 不完整的文件 |

| status | 含义 | 图标 |
|--------|------|------|
| evergreen | 成熟观点，基本定型 | 🌲 |
| growing | 比较成熟但可完善 | 🌱 |
| sprouting | 初稿 / 待打磨 | 🌰 |
| stale | 过时 / 被取代 | 🥀 |

**来源标注（有就标，没有不标）：**
```markdown
## 来源
- [[raw/炒股7年.md]] — 我自己总结的实战纪律
- [[raw/巴菲特2025股东信.md]] — 止损理念受巴菲特影响
```

### you/灵感/ — 灵感与想法（口述专栏）

you/ 的子栏目，专收你的灵感和想法，素材大多来自口述：你在对话里随口说，我整理成条目。

```yaml
---
type: note
status: sprouting
tags: [灵感, 可加主题标签]
来源：口述
---
```

- 命名：`YYYY-MM-DD--灵感--主题.md`，同一天多条加 `-2`、`-3`
- 转写原则：保留你的原始表达，只去口头禅、理语序；拿不准的标 `（待确认）`
- 灵感是种子不是终点：长成文章/方法后，本条标 `stale`，正文头部加 `→ 已长成：[[you/xxx]]`
- 栏目规范：`you/灵感/_栏目说明.md`；索引卡片：`outputs/索引-灵感.md`

### outputs/ — 导航索引（检索入口）

不是存档，是入口卡片。每个卡片链接到 you/（写了什么）和 raw/（引用了谁），标注当时背景和受谁影响。

结构：
```
outputs/
├── __catalog.md（自动生成）
├── 索引-灵感.md
├── 索引-投资体系.md
├── 索引-AI实操体系.md
├── 索引-AI思考.md
├── 索引-自我访谈.md
├── 索引-写书计划.md
├── 索引-知行合一与行动熔炉.md
└── 索引-一人公司与AI产品开发.md
```

---

## 6 条工作流

### 收到外部素材
```
你在飞书/对话中给我一篇文章 → 存 raw/（加 frontmatter）
                                  ↓
问我：这个你用了没？
  ├→ 用了 → 在 you/ 建一条 experience → 更新 outputs/
  └→ 没用 → 就放 raw/，不处理
```

### 你自己写了文章
```
告诉我 → 直接存 you/（不经过 raw/）
           ↓
有引用来源 → 标注 [[raw/xxx]]
           ↓
更新 outputs/ 索引卡片
```

### 听了就用（30 秒记录）
```
你听了一个方法 → 立刻用在实际工作
                  ↓
在 you/ 记一条：
---
type: experience
status: sprouting
---
听了 [来源]，用在 [场景]，产出 [结果]。
---
                  ↓
我会自动关联到已有知识条目
```

### 口述灵感（30 秒随手记）
```
你在对话/飞书里随口说一个想法 → 我整理成条目
                  ↓
you/灵感/YYYY-MM-DD--灵感--主题.md
（type: note | status: sprouting | tags 含 灵感）
                  ↓
自动关联已有知识条目；长成文章/方法后标 stale 并链去向
```

### 跨会话关联（每次对话自动执行）
```
1. 看你在聊什么（投资/AI/小程序/公众号）
2. 搜 you/ 和 raw/ 的相关内容
3. 回复里主动关联：「关于这个你之前还说过……」
```

### 30 天遗忘
```
you/ 里 30 天没修改的 method/article 文件 → 标记为 stale
不删除，可随时复活
you/灵感/ 里 30 天没动的条目 → 也标 stale（灵感会过期）
```

---

## 灵魂锚点：我的个人知识结构

`you/我的个人知识结构.md` 是 vault 的灵魂文件。它不记录别人的东西，它记录：**我知道什么、要去哪、怎么去。**

已有结构（直接参考，无需重写）：
```
知识领域 × 等级定义（核心层→硬核层→支撑层→探索层）
个人目标 × 知识映射矩阵
知识库操作规则
```

当你说"知识库不了解我"或"知识库跟我不对齐"时，去更新这个文件。

---

## 校验规则

### 入库校验（每次写入后）

```python
def validate_entry(filepath):
    errors = []
    # raw/ 不能有自己创作
    if "raw/" in filepath and frontmatter.get("平台") == "自己创作":
        errors.append("raw/ 禁止存自己创作的内容，请移到 you/")
    # you/ 必须有 type 和 status
    if "you/" in filepath:
        if "type" not in frontmatter:
            errors.append("you/ 文件缺少 type 字段")
        if "status" not in frontmatter:
            errors.append("you/ 文件缺少 status 字段")
    # 灵感条目 tags 必须含 灵感
    if "you/灵感/" in filepath and "灵感" not in frontmatter.get("tags", ""):
        errors.append("灵感条目 tags 必须包含 灵感")
    return errors
```

### 遗忘校验（每 30 天）

```bash
# you/ 里 30 天没动的 method/article
for f in you/*.md you/灵感/*.md; do
    days=$(( (date +%s - $(stat -f %m "$f")) / 86400 ))
    if [ $days -gt 30 ]; then
        type=$(head -10 "$f" | grep "^type:" | cut -d' ' -f2)
        status=$(head -10 "$f" | grep "^status:" | cut -d' ' -f2)
        if [ "$type" = "method" -o "$type" = "article" ] && [ "$status" != "stale" ]; then
            echo "⚠️  $f — 30 天未修改，考虑标记 stale"
        fi
    fi
done
```

---

## 信号与应对

| 你说 | 真实需求 | 我的应对 |
|------|---------|---------|
| "帮我存一下这个" | 先收藏，别分析 | 存 raw/ + frontmatter，不问要不要处理 |
| "这个对我有用" | 帮我记住为什么有用 | 存 raw/ + 在 you/ 记一条 experience |
| "我之前写过关于X的" | 帮我找到它 | 先搜 outputs/ → you/ → raw/ |
| "帮我整理一下知识库" | 告诉我现在有什么、缺什么 | 生成目录报告 + 给出具体建议 |
| "知识库不了解我" | 需要更新灵魂文件 | 更新 you/我的个人知识结构.md |
| "我有个灵感" / 口述一段想法 | 随手记下，别弄丢 | 整理口述 → 存 you/灵感/ + 关联已有条目 |

---

## 产出物格式

### 素材入库确认
```
✅ 已存 raw/
   raw/2026-07-14--公众号--xxx.md
   平台：公众号 | 待处理
```

### 知识沉淀确认
```
✅ 已更新 you/
   you/2026-07-14--xxx.md（type: experience | status: sprouting）
   → 关联 [[raw/xxx.md]]
```

### 索引更新确认
```
✅ 已更新 outputs/
   索引-xxx.md → 新增卡片：xxx
```

---

## Gotchas（踩过的坑）

- **you/ 和 raw/ 要同时搜。** 只搜 you/ 会漏掉 raw/ 里还没处理的素材。
- **raw/ 里的"平台：自己创作"是旧架构遗留。** 看到就搬去 you/。
- **批量加元数据不要追加 frontmatter。** 有 frontmatter 的文件在原有块内插入 type/status。追加会导致两个 frontmatter 块，解析错误。
- **大动作用 execute_code 不用 terminal。** `rm -rf` 和 `mv` 目录会被安全拦截。`shutil.move()` 在 execute_code 里可用。
- **30 秒记录规则不是建议是流程。** 你听了就用，30 秒记一条 experience，我会自动关联。
- **30 天遗忘不是删除，是标记 stale。** 知识会过期，旧观点和新观点共存时要有标记区分。
- **口述灵感保留原话。** 只去口头禅、理语序，不做AI腔改写；拿不准的地方标（待确认）。
- **search_files 搜中文文件名会炸。** macOS glob 对中文支持差。用 `python3 -c "os.listdir()"` 替代。
- **CLAUDE.md 不写死人的名字。** 用"你"指代 vault 所有者，不写死"飞叔"或其他身份——CLAUDE.md 是给 AI 的操作手册，不是自我介绍。
- **vault 根目录的 CLAUDE.md 只负责架构和工作流。** 用户画像、项目索引、操作日志规则这些分到 you/ 各自对应的文件里，CLAUDE.md 只做路由和引用。
- **新增文件必须按标准命名 `YYYY-MM-DD--来源分类--主题.md`。** 来源分类从标准值列表选（公众号、自己创作、AI实操、投资、写书、B站等）。不要用 8 位日期（`20260410`）、不要不带日期前缀、不要用 `--` 做多级分类（`--自己-AI应用-` 是旧格式）。frontmatter 里必须加 tags。

---

文档版本：v6.2
最后更新：2026-08-29
